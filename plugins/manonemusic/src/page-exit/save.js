import { useBlockProps } from "@wordpress/block-editor";

export default function Save() {
	// const blockProps = useBlockProps.save({ className });
	return <div className="page-exit fixed inset-0 z-20 bg-secondary"></div>;
}
